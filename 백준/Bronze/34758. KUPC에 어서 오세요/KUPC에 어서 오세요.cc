#include<bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(NULL); 
    ios_base::sync_with_stdio(false);
    int X,Y,N;
    cin>>X>>Y>>N;
    while(N--){
        int x,y;
        cin>>x>>y;
        cout<<(x!=X&&y!=Y)<<"\n";
      
}
  


    return 0;
}