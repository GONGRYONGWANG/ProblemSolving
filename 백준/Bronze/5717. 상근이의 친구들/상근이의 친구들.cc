#include<bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    while(1){
        int a,b;
        cin>>a>>b;
        if(a==0 && b==0) break;
        cout<<a+b<<endl;
    }
    
    
    
    
}